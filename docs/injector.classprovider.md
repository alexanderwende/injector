<!-- Do not edit this file. It is automatically generated by API Documenter. -->

[Home](./index.md) &gt; [injector](./injector.md) &gt; [ClassProvider](./injector.classprovider.md)

## ClassProvider class

A provider for class instances

<b>Signature:</b>

```typescript
export declare class ClassProvider<T> extends BaseProvider<T> 
```

## Constructors

|  Constructor | Modifiers | Description |
|  --- | --- | --- |
|  [(constructor)(constructorFn, parameters, properties)](./injector.classprovider._constructor_.md) |  | Constructs a new instance of the <code>ClassProvider</code> class |

## Remarks

The `ClassProvider` provides instances of classes. It can be created by passing a reference to a class to its constructor. `ClassProvider` automatically analyzes parameter and property dependencies of the class and resolves them when creating instances.

```typescript
@injectable()
class Foo { }

@injectable()
class Bar { }

@injectable()
class FooBar {

    @optional()
    @inject()
    bar!: Bar;

    constructor (public foo: Foo) { }
}

const provider = new ClassProvider(FooBar);
const injector = new Injector();

injector.register(FooBar, provider);

const fooBar = injector.resolve(FooBar)!;

```

